import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.nakuapp.alwaysfreeaac',
  appName: 'AlwaysFreeAAC',
  webDir: 'dist',
};

export default config;
